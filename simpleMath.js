
function simpleAdd(number1, number2) {
    // implement a function that adds two numbers and returns their values
    return parseInt(number1) + parseInt(number2);
}

function absoluteValueSubtract(number1, number2) {
    // implement a function that subtracts two numbers and returns the absolute value
	return 7;
}

function multiplyByTen(number) {
    // implement a function that multiplies what is passed in by ten
}

function generateRandomNumber() {
    // generate a random number between 1 and 42
}

